# 🚀 Руководство по запуску бота

## Для новичков (первый раз)

### Шаг 1: Первая установка
1. **Двойной клик** на `setup.bat`
2. Мастер установки автоматически:
   - ✅ Проверит Python
   - ✅ Установит зависимости
   - ✅ Создаст .env файл
   - ✅ Откроет .env для редактирования

3. **В открывшемся Notepad:**
   ```
   BOT_TOKEN=СЮДА_ВСТАВЬТЕ_ВАШ_ТОКЕН
   ```
   - Замените текст на ваш настоящий токен
   - Сохраните (Ctrl+S) и закройте

4. **Настройте config.json:**
   - Откройте `config.json` в любом редакторе
   - Замените ID на ваши (см. SETUP_GUIDE.md)
   - Сохраните

### Шаг 2: Запуск бота
**Двойной клик** на `start_bot.bat`

Готово! Бот запущен.

---

## Быстрый запуск (после установки)

### Вариант 1: Через BAT файл (проще)
```
Двойной клик на start_bot.bat
```

**Что произойдёт:**
- Зелёное окно CMD откроется
- Автоматически проверятся зависимости
- Бот запустится
- Окно закроется при остановке бота

### Вариант 2: Через PowerShell (красивее)
```
Правой кнопкой на start_bot.ps1 → "Выполнить с помощью PowerShell"
```

**Что произойдёт:**
- Цветной вывод с прогресс-индикаторами
- Более подробная информация
- Автоматическое ожидание при ошибках

---

## Описание файлов

### 🚀 start_bot.bat
**Назначение:** Запуск бота через CMD (простой)

**Функции:**
- Проверка наличия Python
- Проверка .env и config.json
- Автоматическая установка зависимостей (если нужно)
- Запуск бота
- Пауза при ошибке (чтобы прочитать сообщение)

**Использование:**
```
Двойной клик → бот запускается
```

**Остановка бота:**
```
Ctrl+C в окне CMD
```

---

### 🚀 start_bot.ps1
**Назначение:** Запуск бота через PowerShell (продвинутый)

**Преимущества:**
- Цветной вывод (зелёный = OK, жёлтый = предупреждение, красный = ошибка)
- Более подробные проверки
- Красивое форматирование

**Использование:**
```
ПКМ на файл → "Выполнить с помощью PowerShell"
```

**Если не запускается:**
1. Откройте PowerShell от администратора
2. Выполните: `Set-ExecutionPolicy RemoteSigned`
3. Подтвердите: `Y`
4. Попробуйте снова

**Остановка бота:**
```
Ctrl+C в окне PowerShell
```

---

### 🔧 setup.bat
**Назначение:** Мастер первой установки

**Что делает:**
1. Проверяет Python
2. Устанавливает все зависимости
3. Создаёт .env из шаблона
4. Открывает .env для редактирования
5. Проверяет config.json
6. Показывает следующие шаги

**Когда использовать:**
- Первый раз после скачивания бота
- После переустановки Python
- При проблемах с зависимостями

**Использование:**
```
Двойной клик → следуйте инструкциям
```

---

### 📦 install_dependencies.bat
**Назначение:** Только установка зависимостей

**Когда использовать:**
- Обновление discord.py
- После добавления новых зависимостей
- При ошибках импорта

**Использование:**
```
Двойной клик → зависимости установятся
```

---

## Типичные ситуации

### Ситуация 1: Первый запуск
```
1. setup.bat (один раз)
2. Отредактировать .env и config.json
3. start_bot.bat (каждый раз)
```

### Ситуация 2: Каждодневный запуск
```
Двойной клик на start_bot.bat
```

### Ситуация 3: Обновили код
```
1. install_dependencies.bat (если новые зависимости)
2. start_bot.bat
```

### Ситуация 4: Ошибка "module not found"
```
1. install_dependencies.bat
2. start_bot.bat
```

### Ситуация 5: Ошибка "BOT_TOKEN not found"
```
1. Откройте .env в блокноте
2. Проверьте, что токен правильно вставлен
3. Сохраните
4. start_bot.bat
```

---

## Советы по использованию

### ✅ Хорошие практики:

**Создайте ярлык на рабочем столе:**
1. ПКМ на `start_bot.bat` → "Создать ярлык"
2. Перетащите ярлык на рабочий стол
3. Переименуйте: "Запустить Discord бота"

**Автозапуск с Windows:**
1. Win+R → `shell:startup`
2. Создайте ярлык на `start_bot.bat`
3. Бот будет запускаться при включении ПК

**Всегда держите бота запущенным:**
- Используйте планировщик задач Windows
- Или сторонние программы типа PM2

### ⚠️ Частые ошибки:

**"Python не найден"**
- Установите Python с python.org
- При установке ✅ галочку "Add Python to PATH"

**"pip не найден"**
- Переустановите Python с галочкой "pip"

**"Access denied" / "Доступ запрещён"**
- Запустите от администратора
- ПКМ на файл → "Запуск от имени администратора"

**PowerShell скрипт не запускается**
- PowerShell от админа
- `Set-ExecutionPolicy RemoteSigned`

**Окно мгновенно закрывается**
- Используйте `start_bot.bat` вместо `bot.py`
- BAT файл оставляет окно открытым при ошибке

---

## Продвинутое использование

### Запуск в фоновом режиме (без окна)

Создайте `start_bot_silent.vbs`:
```vb
Set WshShell = CreateObject("WScript.Shell")
WshShell.Run chr(34) & "start_bot.bat" & Chr(34), 0
Set WshShell = Nothing
```

Двойной клик → бот запускается без окна.

**Остановка:**
- Диспетчер задач → Завершить процесс `python.exe`

---

### Логирование в файл

Добавьте в конец `start_bot.bat` перед `python bot.py`:
```batch
python bot.py >> bot_log.txt 2>&1
```

Все логи будут записываться в `bot_log.txt`.

---

### Автоперезапуск при крашах

Создайте `start_bot_loop.bat`:
```batch
@echo off
:loop
python bot.py
echo Bot crashed! Restarting in 5 seconds...
timeout /t 5
goto loop
```

Бот автоматически перезапустится при ошибке.

---

## Сравнение методов запуска

| Метод | Сложность | Окно | Авто-проверки | Рекомендуется |
|-------|-----------|------|---------------|---------------|
| `start_bot.bat` | ⭐ Легко | ✅ Да | ✅ Да | ✅ Новичкам |
| `start_bot.ps1` | ⭐⭐ Средне | ✅ Да | ✅ Да | ✅ Опытным |
| `python bot.py` | ⭐⭐ Средне | ✅ Да | ❌ Нет | Для разработки |
| `start_bot_silent.vbs` | ⭐⭐⭐ Сложно | ❌ Нет | ❌ Нет | Для авто-запуска |

---

## Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'discord'"
**Решение:**
```
1. install_dependencies.bat
2. Дождитесь завершения
3. start_bot.bat
```

### Проблема: Бот не реагирует на команды
**Проверьте:**
- Intents включены в Discord Developer Portal
- Префикс команд `!` (не `/`)
- У бота есть права на сервере

### Проблема: Окно закрывается сразу
**Причина:** Ошибка в коде или конфигурации
**Решение:**
- Используйте `start_bot.bat` (оставляет окно открытым)
- Читайте сообщение об ошибке
- Проверьте .env и config.json

---

## Дополнительные ресурсы

- **Полная документация:** `README.md`
- **Настройка каналов:** `SETUP_GUIDE.md`
- **Примеры команд:** `COMMANDS_EXAMPLES.md`
- **Этот файл:** `LAUNCHER_GUIDE.md`

---

Готово! Теперь вы знаете все способы запуска бота. 🚀
