# 🤖 Discord Бот Верификации

Полнофункциональный бот для автоматической верификации пользователей на Discord сервере с тремя уровнями защиты и системой статистики.

## 🚀 Быстрый старт

### Первая установка

1. **Запустите мастер установки:**
   ```
   Двойной клик на setup.bat
   ```

2. **Настройте токен бота:**
   - Откроется `.env` файл
   - Замените `ваш_новый_токен_здесь` на реальный токен
   - Сохраните (Ctrl+S)

3. **Настройте config.json:**
   - Укажите ID вашего сервера, ролей и каналов
   - Как получить ID - см. раздел "Настройка каналов"

4. **Запустите бота:**
   ```
   Двойной клик на start_bot.bat
   ```

### Каждодневный запуск

```
Двойной клик на start_bot.bat или start_bot.ps1
```

---

## ⚙️ Настройка каналов и ролей

### Получение ID

1. **Включите режим разработчика в Discord:**
   - Настройки → Расширенные → Режим разработчика (включить)

2. **Копирование ID:**
   - Правой кнопкой мыши на канал/роль → "Копировать идентификатор"

### Создание ролей

Создайте две роли в Настройки сервера → Роли:

**🔴 Неверифицирован** (UNVERIFIED_ROLE_ID)
- Права: Может видеть только канал приветствий
- Не может писать сообщения

**🟢 Верифицирован** (VERIFIED_ROLE_ID)
- Права: Полный доступ к серверу

⚠️ **ВАЖНО:** Роль бота должна быть ВЫШЕ обеих ролей в иерархии!

### Создание каналов

**Вариант А: Три отдельных канала (рекомендуется)**

1. **📢 #приветствия** (WELCOME_CHANNEL_ID)
   - Только бот может писать, все могут читать

2. **📋 #логи-верификаций** (LOG_CHANNEL_ID)
   - Только модераторы видят, только бот пишет

3. **🛡️ #модерация** (MODERATOR_CHANNEL_ID)
   - Только модераторы, для ручной проверки (уровень 3)

**Вариант Б: Один канал для всего (маленькие серверы)**
- Используйте один ID для всех трёх параметров в config.json

### Пример config.json

```json
{
  "GUILD_ID": 12312312312,
  "UNVERIFIED_ROLE_ID": 123123123,
  "VERIFIED_ROLE_ID": 123123123123,
  "MODERATOR_CHANNEL_ID": 123123123123123,
  "WELCOME_CHANNEL_ID": 123123123123123123,
  "LOG_CHANNEL_ID": 123123123123123123,
  "VERIFICATION_LEVEL": 2
}
```

### Права бота

Минимальные необходимые права:
- ✅ Управление ролями (Manage Roles)
- ✅ Кик участников (Kick Members) - для уровня 3
- ✅ Отправка сообщений
- ✅ Встраивание ссылок
- ✅ Чтение истории сообщений

### Discord Developer Portal

Включите в Bot → Privileged Gateway Intents:
- ✅ Server Members Intent
- ✅ Message Content Intent

---

## 📋 Уровни верификации

### Уровень 1 - Простая команда
Пользователь вводит `!verify` для верификации.

### Уровень 2 - QR-код (текущий)
Пользователю отправляется QR-код в ЛС. Нужно отсканировать и ввести код командой `!code КОД`.

### Уровень 3 - Ручная модерация
Модераторы вручную одобряют или отклоняют каждого нового пользователя через кнопки.

---

## 📖 Примеры использования

### Для пользователей

**Уровень 1 - Простая команда:**
```
1. Зайти на сервер
2. Написать: !verify
3. Готово! Роль обновлена
```

**Уровень 2 - QR-код:**
```
1. Зайти на сервер
2. Проверить ЛС от бота (QR-код)
3. Отсканировать QR-код
4. Написать боту в ЛС: !code ВАШ_КОД
5. Готово!
```

**Потеряли код? Напишите боту:**
```
!resendcode
```

**Уровень 3 - Ручная модерация:**
```
1. Зайти на сервер
2. Ждать проверки модератора
3. Модератор одобрит/отклонит через кнопки
```

### Для администраторов

**Сменить уровень верификации:**
```
!setlevel 1    # Простая команда
!setlevel 2    # QR-код (рекомендуется)
!setlevel 3    # Ручная модерация
```

### Для модераторов (статистика)

```
!stats          # Статистика за неделю
!stats 30       # Статистика за месяц
!verifstats     # Подробная статистика + топ модераторов
!recent         # Последние 10 верификаций
!recent 25      # Последние 25 верификаций
!checkuser @юзер # Проверка пользователя
```

---

## 🔧 Команды

### Команды верификации
- `!setlevel <1-3>` - Изменить уровень верификации (только администраторы)
- `!verify` - Верификация (только уровень 1)
- `!code <КОД>` - Ввести код (только уровень 2, в ЛС)
- `!resendcode` - Повторно отправить QR-код (только уровень 2, в ЛС)

### Команды статистики 📊
- `!stats [дни]` - Общая статистика сервера за период (по умолчанию 7 дней)
- `!verifstats` (или `!vstats`) - Подробная статистика верификаций
- `!recentverif [количество]` (или `!recent`) - Последние верификации (по умолчанию 10)
- `!checkuser [@пользователь]` (или `!userinfo`) - Детальная информация о пользователе

## ✨ Новые возможности

### 📊 Модуль статистики и аналитики
Полноценная система сбора и анализа данных о верификациях:
- **Автоматический сбор** - все верификации логируются в SQLite БД
- **Детальная статистика** - по периодам (7, 30, 90+ дней)
- **Анализ эффективности** - процент успешных верификаций
- **Топ модераторов** - рейтинг самых активных модераторов
- **История пользователей** - полная информация о каждом участнике
- **Предупреждения** - автоматическое выявление молодых аккаунтов

Используйте команду `!stats` для просмотра статистики.

### �📨 Автоматические приветственные сообщения
При входе нового пользователя бот отправляет красивое приветственное сообщение с инструкциями по верификации в канал `WELCOME_CHANNEL_ID`. Сообщение автоматически адаптируется под текущий уровень верификации.

### 📊 Логирование верификаций
Все события верификации логируются в канал `LOG_CHANNEL_ID`:
- ✅ Успешные верификации (с указанием метода)
- ❌ Отклонённые пользователи (с указанием модератора)
- Информация о пользователе: аватар, имя, ID, дата создания аккаунта
- Метки времени для каждого события

**Методы верификации в логах:**
- `команда` - через `!verify` (уровень 1)
- `qr-код` - через `!code` (уровень 2)
- `модератор` - ручное одобрение/отклонение (уровень 3)

### 🔄 Повторная отправка QR-кода
Если пользователь потерял код или случайно закрыл ЛС, он может написать боту команду `!resendcode`:
- Бот отправит существующий код, если он ещё активен
- Или создаст новый, если код был удалён
- Команда работает только в личных сообщениях
- Доступна только при уровне верификации 2
- Автоматически проверяет, не верифицирован ли уже пользователь

## ⚠️ Важные требования

1. **Иерархия ролей**: Роль бота должна быть выше ролей верификации
2. **Права бота**: Manage Roles, Kick Members
3. **Права канала**: Бот должен иметь доступ к каналу модерации

---

## 🐛 Устранение неполадок

### Бот не запускается

**Ошибка: "Python не найден"**
- Установите Python с python.org
- При установке ✅ галочку "Add Python to PATH"

**Ошибка: "BOT_TOKEN not found"**
- Проверьте файл `.env`
- Убедитесь, что токен скопирован без пробелов
- Формат: `BOT_TOKEN=ваш_токен_здесь`

**Ошибка: "ModuleNotFoundError"**
```
Двойной клик на install_dependencies.bat
```

### Бот онлайн, но не работает

**Команды не отвечают:**
- Проверьте префикс: `!verify` (не `/verify`)
- Включите Intents в Discord Developer Portal
- Убедитесь, что бот имеет права на сервере

**Не может выдать роли:**
- Роль бота должна быть ВЫШЕ ролей верификации
- У бота должно быть право "Управление ролями"

**Не приходит QR-код в ЛС:**
- Пользователь должен разрешить ЛС от участников сервера
- Проверьте: Настройки конфиденциальности → Личные сообщения

**Используйте команду для повторной отправки:**
```
!resendcode (в ЛС боту)
```

### Проблемы с логами

**Логи не появляются:**
- Проверьте права бота в канале логов
- Убедитесь, что LOG_CHANNEL_ID правильный
- Бот должен иметь право "Отправка сообщений"

**Приветствия не отправляются:**
- Проверьте права бота в канале приветствий
- Убедитесь, что WELCOME_CHANNEL_ID правильный

### Статистика не работает

**Команды статистики не отвечают:**
- У вас должны быть права "Управление сервером" (для !stats)
- У вас должны быть права "Управление ролями" (для !checkuser)

**Нет данных в статистике:**
- Это нормально для нового бота
- Данные появятся после первых верификаций

**База данных повреждена:**
```
1. Остановите бота
2. Переименуйте verification_stats.db в backup.db
3. Запустите бота (создастся новая БД)
```

### Тестирование

**Создайте тестовый аккаунт Discord и:**
1. Зайдите на сервер
2. Проверьте приветственное сообщение
3. Пройдите верификацию
4. Проверьте логи
5. Проверьте статистику

---

## 🔒 Безопасность

- ✅ Токен хранится в `.env` (не в коде)
- ✅ Проверка прав модераторов
- ✅ Обработка ошибок для всех операций
- ✅ `.gitignore` защищает от случайной публикации секретов

## 📝 Структура проекта

```
glistbot/
├── bot.py                      # 🤖 Главный файл бота
├── verification_cog.py         # 🔐 Модуль верификации
├── stats_cog.py               # 📊 Модуль статистики
├── test_stats.py              # 🧪 Тестирование БД
│
├── config.json                # ⚙️ Настройки (ID, уровень)
├── .env                       # 🔑 Токен бота (секретный!)
├── .env.example               # 📋 Шаблон для .env
│
├── verification_stats.db      # 🗄️ База данных (авто)
│
├── requirements.txt           # 📦 Зависимости Python
├── .gitignore                 # 🚫 Исключения Git
│
├── start_bot.bat             # 🚀 Запуск (CMD)
├── start_bot.ps1             # 🚀 Запуск (PowerShell)
├── setup.bat                 # 🔧 Первая установка
├── install_dependencies.bat  # 📦 Установка зависимостей
│
└── README.md                  # 📖 Документация
```

### Описание файлов

**Код:**
- `bot.py` - точка входа, загрузка модулей
- `verification_cog.py` - 3 уровня верификации
- `stats_cog.py` - статистика и аналитика

**Настройка:**
- `config.json` - ID сервера/ролей/каналов
- `.env` - токен Discord бота

**Запуск:**
- `setup.bat` - первая установка
- `start_bot.bat` - обычный запуск

## 🐛 Решение проблем

**Бот не запускается:**
- Проверьте, что токен правильно указан в `.env`
- Убедитесь, что установлены зависимости: `pip install -r requirements.txt`

**Бот не может выдать роли:**
- Проверьте иерархию ролей (роль бота должна быть выше)
- Убедитесь, что у бота есть права "Manage Roles"

**Не приходит QR-код:**
- Пользователь должен разрешить ЛС от участников сервера
- Проверьте, что уровень верификации = 2
- Используйте команду `!resendcode` в ЛС бота для повторной отправки

**Потерян код верификации:**
- Напишите боту в ЛС команду `!resendcode`
- Бот отправит ваш текущий код или создаст новый

## 📄 Лицензия

Свободное использование для личных проектов.
